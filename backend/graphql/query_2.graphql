query PostBySlug($host: String!, $slug: String!) {
  publication(host: $host) {
    post(slug: $slug) {
      title
      readTimeInMinutes
      content {
        markdown
      }
      publishedAt
      updatedAt
    }
  }
}
